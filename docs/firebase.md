# firebase関連のドキュメント

firebase CLI toolsはdevcontainerにインストール済みです。
以下コマンドからfirebaseにログインします。

```shell
firebase login
```

`npm run build`で`frontend/dist`配下にビルドファイルが展開され、`firebase deploy`で最新環境にデプロイされる。
