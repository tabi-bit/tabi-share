/**
 * pill (Mobile / Desktop) の共通表面スタイル。
 * `bg-white/75 backdrop-blur-md` で Header の teal 色と別レイヤー化し、
 * `shadow-lg` + `ring-1 ring-white/60` で浮遊感を出す。
 */
export const PILL_SURFACE_CLASSES = 'bg-white/75 shadow-lg backdrop-blur-md ring-1 ring-white/60';
